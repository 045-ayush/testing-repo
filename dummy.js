neiwninwindinewinidnwe// Basic Node structure 
class Node {
  constructor(value, next = null) {
    this.value = value;
    this.next = next; 
  }
}

// Example usage to create a simple linked list
let head = new Node(1);
head.next = new Node(2);
head.next.next = new Node(3);






trying pull command

